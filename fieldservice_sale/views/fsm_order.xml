<odoo>
    <record id="view_fsm_order_form_inherit_sale" model="ir.ui.view">
        <field name="model">fsm.order</field>
        <field name="inherit_id" ref="fieldservice.fsm_order_form" />
        <field name="arch" type="xml">
            <xpath expr="//div[hasclass('oe_button_box')]" position="inside">
                <button
                    type="object"
                    name="action_view_sales"
                    class="oe_stat_button"
                    icon="fa-usd"
                    invisible="not sale_line_id and not sale_id"
                    groups="sales_team.group_sale_salesman"
                >
                    <span class="o_stat_text">Sale Order</span>
                </button>
            </xpath>
            <xpath expr="//header" position="inside">
                <button
                    name="action_create_quotation"
                    type="object"
                    string="Create Quotation"
                    class="btn-primary"
                    invisible="sale_id or is_closed or stage_id.is_default"
                    groups="fieldservice.group_fsm_user"
                />
            </xpath>
        </field>
    </record>
</odoo>
